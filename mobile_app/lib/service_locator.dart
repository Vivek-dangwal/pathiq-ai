import 'package:get_it/get_it.dart';

final getIt = GetIt.instance;

void setupServiceLocator() {
  // Services will be registered here as we build them in later milestones
  // Example (not yet used): getIt.registerLazySingleton<SomeService>(() => SomeService());
}